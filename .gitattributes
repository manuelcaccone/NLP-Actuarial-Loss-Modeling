# Forza le interruzioni di linea in stile Unix (LF) per i file di testo
# Questo include anche i file .md come le fonti fornite
*.md text eol=lf
*.py text eol=lf
*.csv text eol=lf
*.txt text eol=lf
*.json text eol=lf
*.xml text eol=lf
*.html text eol=lf

# Abilita il diff e il merge specifico per i notebook Jupyter (.ipynb)
# Richiede l'installazione di nbdime o strumenti simili
*.ipynb diff=ipynb