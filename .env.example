# This file serves as an example for the .env file that MUST be created.
# It contains the environment variables required to run the project.
# COPY this file and RENAME it to .env in the root directory of the project.
# Replace the placeholder values ("...") with your actual credentials and configurations.
# The .env file (the real one, NOT .env.example) MUST NOT BE ADDED TO GIT
# for security reasons, as it contains sensitive information like API keys.

# --- OpenAI API Configuration ---
# API key for accessing OpenAI services.
# Required for features such as text augmentation and Q&A pair generation.
# You can obtain your API key from your OpenAI account dashboard.
# Using environment variables is recommended for security.
OPENAI_API_KEY="your_openai_api_key_here"

# --- Weights & Biases Configuration (Optional but recommended) ---
# API key for integration with Weights & Biases (wandb).
# Used for tracking and monitoring model fine-tuning experiments.
# If you use wandb, get this key from your account settings.
# If you don't use wandb, you can omit these variables or leave them empty.
# WANDB_API_KEY="your_wandb_api_key_here"

# Name of the project on Weights & Biases to which data will be sent.
# WANDB_PROJECT="your_wandb_project_name"

# Name of the entity (usually your username or team name) on Weights & Biases.
# WANDB_ENTITY="your_wandb_entity"

# --- Other configurations (Add any additional required environment variables here) ---
# ... for example, paths to sensitive data, credentials for other services, etc.
